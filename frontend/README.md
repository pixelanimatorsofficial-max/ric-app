# Municipal Waste Management Frontend

## Local setup

1. Copy `.env.example` to `.env`.
2. Fill the backend URL, Supabase URL, and Supabase anon key.
3. Install and run:

```bash
npm install
npm run dev
```

The supervisor form is at `/`. The protected admin dashboard is at `/admin` and uses Supabase Auth email/password users.
